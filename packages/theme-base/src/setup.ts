/**
 * Theme setup and initialization
 * Imports all styles and prepares the theme module
 */

// Import styles
import "./styles/variables.css";
import "./styles/global.css";

// This function can be called to initialize the theme
export function setupTheme(): void {
  // Theme initialization logic
  // Styles are already imported and applied via CSS
}
