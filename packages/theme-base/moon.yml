language: typescript
layer: library
toolchains:
  default: 'bun'

dependsOn:
  - id: 'schema'
    scope: 'build'

tasks:
  install:
    command: 'bun install'
    inputs:
      - 'package.json'
    outputs:
      - 'node_modules'
    options:
      cache: false
