language: typescript
stack: frontend
layer: application
toolchains:
  default: 'bun'

dependsOn:
  - id: 'theme-base'
    scope: 'build'

tasks:
  install:
    command: 'bun install'
    inputs:
      - 'package.json'
    outputs:
      - 'node_modules'
    options:
      cache: false
